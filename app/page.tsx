'use client'; import { useEffect, useState } from 'react'; import { supabase } from '../lib/supabase'; export default function Dash(){ const [contas,setContas]=useState<any[]>([]); useEffect(()=>{ supabase.from('contas').select('*').then(({data})=> data && setContas(data)) },[]); return <div><div className='bg-white border-b px-8 py-5'>Dashboard</div><div className='p-6 grid grid-cols-4 gap-4'>{contas.length} contas</div></div> }