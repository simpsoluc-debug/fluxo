'use client';import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabase';export default function Cli(){const[list,setList]=useState<any[]>([]);const[n,setN]=useState('');useEffect(()=>{supabase.from('contatos').select('*').eq('tipo','cliente').then(r=>r.data&&setList(r.data))},[]);async function add(){if(!n)return;const {data}=await supabase.from('contatos').insert([{nome:n,tipo:'cliente'}]).select();if(data){setList([...list,data[0]]);setN('')}}return <div className='p-6'><h2 className='text-2xl font-bold mb-6'>Clientes</h2><div className='flex gap-2 mb-6'><input value={n} onChange={e=>setN(e.target.value)} className='input flex-1' placeholder='Nome'/><button onClick={add} className='px-4 py-3 bg-emerald-500 text-black rounded-xl font-bold'>Add</button></div><div className='grid md:grid-cols-3 gap-3'>{list.map(f=><div key={f.id} className='card'>{f.nome}</div>)}</div></div>}