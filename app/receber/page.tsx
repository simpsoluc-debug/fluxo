'use client'; import { useEffect, useState } from 'react'; import { supabase } from '../../lib/supabase'; export default function R(){ const [c,setC]=useState<any[]>([]); useEffect(()=>{ supabase.from('contas').select('*').eq('tipo','receber').then(({data})=> data && setC(data)) },[]); return <div><div className='bg-white border-b px-8 py-5'>Contas a receber</div><div className='p-6'><div className='card p-4'>{c.length} contas</div></div></div> }